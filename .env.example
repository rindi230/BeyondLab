# Django Backend URL
# Vite requires client env vars to start with VITE_
VITE_DJANGO_API_URL=http://localhost:8000/api

# Supabase (if still using)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_key
